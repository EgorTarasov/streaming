package framer

// Framer commands
const (
	Status   = "status"
	Add      = "add"
	Remove   = "remove"
	Shutdown = "shutdown"
)

// Framer response msg statuses
const (
	StatusMsgType   = "status"
	ResponseMsgType = "response"
	HealthCheck     = "healthcheck"
)
