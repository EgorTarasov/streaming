package status

// Task statuses (PENDING, PROCESSING, DONE, ERROR) matching the task_status type in the database
const (
	PENDING    = "PENDING"
	PROCESSING = "PROCESSING"
	DONE       = "DONE"
	ERROR      = "ERROR"
)
